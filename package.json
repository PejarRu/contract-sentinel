{
  "name": "contract-sentinel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only auditor for newly deployed Ethereum protocols/tokens",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test",
    "once": "tsx src/index.ts --once",
    "watch": "tsx src/index.ts --watch"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3",
    "tsx": "^4.19.0",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0",
    "@types/node": "^22.7.0",
    "typescript": "^5.6.0"
  }
}
